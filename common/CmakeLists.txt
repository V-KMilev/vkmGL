message("========= [BUILDING] Common =========")

set(SOURCES
	${CMAKE_CURRENT_SOURCE_DIR}/error/error_handle.cpp
	${CMAKE_CURRENT_SOURCE_DIR}/files/file_read.cpp
	${CMAKE_CURRENT_SOURCE_DIR}/object_id/object_id.cpp
)


# Set the target's include directories
target_include_directories(${PROJECT_NAME} PRIVATE
	${CMAKE_CURRENT_SOURCE_DIR}/error
	${CMAKE_CURRENT_SOURCE_DIR}/files
	${CMAKE_CURRENT_SOURCE_DIR}/object_id
)

# Add the source files to the target
target_sources(${PROJECT_NAME} PRIVATE ${SOURCES})
